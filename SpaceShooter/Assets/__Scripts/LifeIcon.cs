using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//****could not get this to work with my code****
public class LifeIcon : MonoBehaviour
{
     // need a life prefab for the icons
    //[SerializeField] private LifeIcon lifeIconPrefab;

    //need a GameController - find in start
    //private PlayerMovement player;
    //private int startingLives;
    //private List<LifeIcon> icons = new List<LifeIcon>();

    // Start is called before the first frame update
    //private void Start()
//     {
//         player = FindObjectOfType<PlayerMovement>();
//         if(player)
//         {
//                 startingLives = player.PlayerLives;  
//                 CreateIcons();
//         }
              
//     }

//     private void CreateIcons()
//     {
//         for(int i = 0; i < startingLives; i++)
//         {
//             LifeIcon lifeIcon = instantiate(lifeIconPrefab, transform);
//             icons.Add(lifeIcon);
//         }
//     }
}
